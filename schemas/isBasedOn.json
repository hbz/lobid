{
  "$id": "resource:/isBasedOn.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "isBasedOn",
  "description": "Entities e.g. products that are used in the described entity",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "id": {
        "$ref": "resource:/id.json"
      },
      "type": {
        "type": "string",
        "enum": [
          "Software",
          "Data"
        ]
      },
      "name": {
        "$ref": "resource:/name.json"
      }
    },
    "required": [
      "id"
    ]
  }
}