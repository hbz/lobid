{
  "$id": "resource:/slogan.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Slogan",
  "description": "The slogan or subtitle of the entity. MUST have german and english version",
  "allOf": [
    { "$ref": "localizedString.json" },
    { "required": ["en", "de"]}
  ]
}